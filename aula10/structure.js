var container = document.querySelector('.container');

var labelNameElement = document.createElement("label");
labelNameElement.appendChild(document.createTextNode("Nome"));

container.appendChild(labelNameElement);